node_modules/
.env
.env.local
.next/
*.log
.DS_Store

# Widget dist is committed so the API can serve it without a build step
!apps/widget/dist/
.claude/

# Conversas locais — não subir no repositório
conversations/
